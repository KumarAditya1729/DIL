// Missing jspdf due to network issues
export const generateReceiptPDF = (receiptData: any) => {
  alert("PDF generation is mocked. Download would happen here for Receipt " + receiptData.receiptNumber + " from Dance Is Life Art & Study Center");
};
